---
type: concept
status: developing
area: growth
tags: [learning, SFT, 指令微调, 数据集]
created: 2026-06-01
updated: 2026-06-01
---

# SFT 与指令微调

> 本页讲清四件事:有监督/无监督是什么、SFT 是什么、指令微调为什么存在、微调还有哪些别的形式。

## 一、先搞懂:有监督 / 无监督 / 自监督

区别就一句话:**训练数据里有没有"标准答案"。**

| 类型 | 有没有标准答案 | 类比 | 例子 |
|---|---|---|---|
| **有监督 Supervised** | **有**,每条数据配人给好的正确输出 | 有老师批改作业 | SFT、指令微调 |
| **无监督 Unsupervised** | **没有**,只给原始数据自己找规律 | 自己看书自学 | 聚类 |
| **自监督 Self-Supervised** | 答案**藏在数据自身里**(无监督的一种) | 遮住后半句猜前半句 | 大模型**预训练** |

> [!key-insight] 一个完整流程串起来
> **预训练(自监督,海量无标注文本,学"预测下一个词")→ SFT/指令微调(有监督,人工标注的"指令-回答"对,学"听指挥")。**
> 先靠自监督学会语言和知识,再靠有监督学会听懂并执行人类指令。

## 二、SFT 是什么

**SFT(Supervised Fine-Tuning,有监督微调)**:给模型看"输入 → 标准答案"的标注数据,让它照着学,从而更好地执行特定任务。"有监督"= 每条数据都有人给好的标准答案。

## 三、指令微调:为什么存在(精髓)⭐

**指令微调(Instruction Tuning)是 SFT 的一种特殊形式**,数据全是"**人类指令 + 期望回答**"的配对。它要解决预训练模型的一个尴尬毛病:

预训练模型只学了一件事 —— **"预测下一个词"**。它很会"接话",但**不理解"指令"这个概念**。

比如给一个没做过指令微调的模型输入:

> "请帮我把这句话翻译成英文:我爱学习"

它可能不翻译,反而**接着你的话续写**:

> "请帮我把这句话翻译成英文:我爱学习,这是老师布置的作业,我还要……"

因为在它眼里,你这句话只是一段文本的开头,本能是"续写",**而不是"执行命令"**。

> [!key-insight] 指令微调到底教会了什么
> **"看到一条指令时,我应该去『完成这个任务』,而不是『把这段话续写下去』。"**
> 训练后,同样的输入它会规矩地回:`I love studying.`
> 我们今天用的 ChatGPT、各种对话助手能听懂"帮我写封邮件""总结这段话",**全靠指令微调这一步**,把"续写机器"变成"能听指挥的助手"。

> [!key-insight] 关系
> 指令微调 ⊂ SFT。区别在数据结构:指令微调强调"人类指令 + 期望输出"的配对。

## 四、除了指令微调,还有哪些微调

按"用什么数据、教什么"分,常见的有:

| 微调类型 | 数据/信号 | 教模型什么 | 属于 |
|---|---|---|---|
| **指令微调** | 指令 → 期望回答 | 听懂并执行人类指令 | 有监督(SFT) |
| **对话微调** | 多轮对话记录 | 维持上下文、多轮聊天 | 有监督(SFT) |
| **领域微调** | 某行业语料(医疗/法律/代码) | 注入专业知识 | 有监督(SFT) |
| **RLHF(人类反馈强化学习)** | 人类对多个回答的**偏好排序** | 回答更符合人类喜好/价值观 | 强化学习,**非 SFT** |
| **DPO(直接偏好优化)** | 成对的"好回答/坏回答" | 同上,但比 RLHF 简单 | 偏好优化,**非 SFT** |

> [!key-insight] 两个大阶段串起来
> 主流大模型(如 ChatGPT)训练后半段通常是:
> **① SFT / 指令微调**(有监督,先教会"听指令、能回答")→ **② RLHF / DPO**(偏好优化,再教会"回答得让人满意、安全得体")。
> - 指令微调解决"**会不会做**"(能否听懂指令、给出回答)。
> - RLHF/DPO 解决"**做得好不好**"(选出人类更喜欢、更安全的回答)。

## 五、指令数据集格式

- **Alpaca**:Instruction / Input / Response,应用最广。
- **ChatML**:`<|im_start|>...` 对话系统优化。
- 简单格式:Instruction / Input / Output。

## 六、数据构建原则

> [!key-insight] 质量 > 数量
> 高质量指令数据直接决定模型表现与能力边界。

- 四要素:多样性、高质量、一致性、可扩展性。
- 质量评分:相关性 30% + 准确性 30% + 清晰度 20% + 安全性 20%,阈值 ≥0.6。
- 流程:需求分析 → 收集/生成 → 清洗 → 质量筛选 → 增强 → 划分 → 输出。
- 策略:从几百条小规模起步,逐步扩展。

## 继续学习

- [[大模型微调概述]]
- [[全量微调与高效微调]]
- [[LoRA 与 QLoRA]]
- [[SFT数据集构建完全指南]]
- 综合页:[[Research: 大模型微调]]
