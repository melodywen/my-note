---
type: index
status: active
area: growth
tags: [learning, dsh, DeepSeek, cordis, 深水区, 路线图, 有出处]
created: 2026-09-16
updated: 2026-09-16
---

# 深水区 · 学习顺序与依赖关系

> [!info] 版本锚点
> - 来源：`dsh-learn/AGENTS.md`「深水区」节（对应课程编排篇 13–17、实战篇 18–21）
> - 源码：`~/ai-work/dsh/deepseek-harness/`（master `0d1f50007f`）
> - **本页是深水区的学习导航**——先定顺序，再逐块学

## 深水区 = 8 大块（AGENTS.md 原文）

| # | 块 | 内容 | 官方材料 |
|---|---|---|---|
| 1 | **内核机制** | agent-loop 循环、Append-Only Log 崩溃恢复、invariants、防御性模式 | `subsystems/core`（1265 行）、`defensive-patterns`（35 行） |
| 2 | **任务编排与多 Agent** | Agent Loop、Goal 驱动续跑、Subagent、Workflow vs Ralph、agent-team | `subsystems/{subagent(779),agent-team(207),goal(313),workflow(278)}` |
| 3 | **远程调用与多端** | Typert 远程调用协议（Host↔Client）、Web Client 架构 | `subsystems/typert`、`web-client` |
| 4 | **Web UI 定制** | client-ui 插件家族、`--dsw-*` token、样式体系 | `web-styling`（30 行）+ client 包 |
| 5 | **代码考古学** | graph-atlas、module-graph、session-format-status、cookbook | `graph-atlas`(24)、`module-graph`、cookbook |
| 6 | **测试与基准** | testing 体系、benchmarks、minimal 评测 | `testing.zh.md` |
| 7 | **mini-dsh 从零实现** | 亲手写迷你 harness（综合检验） | （需自己组织） |
| 8 | **Agent 自进化 + 生态贡献** | 规则引擎、异常驱动进化、社区插件、postmortem、提 PR | `postmortem/`、社区 |

> **另有 `practice/dynamic-cordis`**（官方实战）：Agent 运行时挂载模型自己写的插件——它是块 8 的雏形，**不在深水区 8 块里但强相关**。

## 依赖关系（关键）

```
        ┌────────────────────────┐
        │ 1. 内核机制（agent-loop）│ ← 地基：所有块的基础
        │    core 子系统           │
        └──────┬──────┬──────┬────┘
               │      │      │
     ┌─────────┘      │      └──────────┐
     ▼                ▼                 ▼
2. 多 Agent       8. 自进化        7. mini-dsh
（依赖内核）      （依赖内核+多Agent）（= 重写内核，综合检验）
     ▲
     │
  dynamic-cordis（依赖内核 + Cordis 内核）
```

**依赖结论**：
- **块 1（内核）是地基**——2 / 7 / 8 / dynamic-cordis **都建在它之上**
- **块 2（多 Agent）最大最实用**（subagent 779 行）——但依赖块 1
- **块 7（mini-dsh）= 重写地基**，必须在块 1 之后
- **块 4/5/6（Web UI / 考古 / 测试）较独立**——可穿插，不依赖块 1

## 学习顺序（本系列将按此推进）

| 顺序 | 块 | 理由 | 状态 |
|---|---|---|---|
| **①** | **内核机制**（agent-loop） | **地基**，必先 | 🔄 进行中 |
| ② | dynamic-cordis | 轻、酷、用起 Cordis 内核（阶段一） | ⬜ |
| ③ | 多 Agent 协作 | 建在①上；最实用、最大 | ⬜ |
| ④ | mini-dsh 从零 | 综合检验（重写①） | ⬜ |
| ⑤ | 远程调用与多端（Typert/Web Client） | 依赖① | ⬜ |
| ⑥ | Web UI 定制 / 测试基准 | 较独立 | ⬜ |
| ⑦ | 代码考古 | 独立 | ⬜ |
| ⑧ | Agent 自进化 + 生态贡献 | 依赖②③ | ⬜ |

> **可穿插**：块 4/5/6（不依赖①），横向技能（提示词/可观测性/安全/成本）随做随学。

## 本篇（深水区）文档规划

```
深水区/
├── 00 学习顺序与依赖关系.md      ← 本页
├── 01 内核机制——agent-loop 循环.md
├── 02 dynamic-cordis——运行时自扩展.md
├── 03 多 Agent 协作——Subagent 与 Workflow.md
├── 04 mini-dsh——从零写一个迷你 harness.md
└── ...
```

## 下一步

- **开始学 ① 内核机制**（`subsystems/core.zh.md` 1265 行，最大文档）
- 逐块推进，每块先搜材料 → 讲透 → 能动手则动手
