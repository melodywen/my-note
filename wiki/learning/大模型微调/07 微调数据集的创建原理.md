---
type: concept
status: developing
area: growth
tags: [learning, 微调, 数据集, dataset, function-calling]
created: 2026-06-01
updated: 2026-06-01
---

# 微调数据集的创建原理

> 本页讲 **微调数据集**:为什么它是微调成败的"头号关键因素",什么时候能直接拿现成数据集、什么时候必须自己手动造。一句话记住:**模型怎么练,练成什么样,八成由数据集决定 —— 框架只是工具,数据才是灵魂。**

## 一句话定位

**如何创建和选取微调数据集,是决定模型微调效果成败的最关键因素。** [[01 四大微调框架概述|微调框架]]决定"用什么工具训",而**数据集决定"教会模型什么"**。

> [!key-insight] 框架是锅,数据是菜
> 同样一口好锅([[03 LLaMA-Factory|LLaMA-Factory]] / [[04 ms-SWIFT|ms-SWIFT]]),炒出来好不好吃,取决于你放的菜(数据)。
> **垃圾进,垃圾出(Garbage in, garbage out)** —— 再强的框架也救不了一份脏乱差的数据集。

## 大多数情况:选框架 + 配现成数据集就够

截止目前,业界已经诞生了**各类微调框架** + **海量公开微调数据集**。绝大多数情况下,你只需要:

```
选一个微调框架(LLaMA-Factory / ms-SWIFT ...)
        +
挑一个合适的公开数据集(Alpaca / ShareGPT / 领域开源数据 ...)
        ↓
直接开训
```

这条路最省事,适合入门和大多数通用任务。

## 什么时候现成数据集不够、必须手动造

当模型能力越来越复杂,公开数据集可能就**满足不了要求**了。典型场景:

| 场景 | 为什么现成数据集不够 |
|---|---|
| **复杂功能微调** | 现在很多模型具备 **Function Calling(工具调用)** 甚至**推理 / 混合推理**能力。例如想给 Qwen3 微调 Function Calling 能力、**同时还要保留它的混合推理能力** —— 这种"既要又要"的组合数据,公开集很少现成提供。 |
| **特定领域知识** | 想让模型专注某个垂直领域(医疗、法律、内部业务),通用公开数据覆盖不到你的领域知识。 |
| **特殊工具调用准确率** | 想提升模型对**某套特定工具组**的调用准确率,必须用贴合这套工具的数据去教它。 |

> [!key-insight] 判断要不要自己造数据的简单标准
> 问自己一句:**"我要教模型的这件事,网上有没有现成、对口的数据?"**
> - 通用对话 / 常见任务 → 大概率有,直接用公开集。
> - 你公司内部流程、你自定义的工具、"既要 A 又要保留 B"的复合能力 → 大概率没有,**得手动创建或合并数据集**。

## 手动造数据集,先懂原理

而要**手动合并或创建**微调数据集,就必须**深入了解数据集构造背后的原理** —— 数据长什么格式、字段怎么组织、对话/工具调用怎么编码,都直接影响模型能否学对。

> 这正是后续内容要展开的:**创建微调数据集背后的底层原理**(数据格式、字段结构、如何编码 Function Calling 与推理过程等)。本篇先建立"为什么要造数据"的全局认知。

## 底层原理一:模型内置的特殊字符与提示词模板

最快理解"数据集该长什么样"的方法,就是**从模型底层怎么读输入入手**。

当代大模型普遍要靠一些**特殊字符(special tokens)**来标记:谁在说话(用户 / 系统 / 助手)、系统提示词、工具调用、多模态输入等。**你以为的对话**和**模型真正看到的输入**其实差很远。

### 你看到的 vs 模型实际看到的(以 Qwen3 为例)

一次最简单的问答:你输入"你好!",模型回"你好呀,很高兴见到你!"

**(1)我们看到的样子** —— 干净的一问一答:

```
用户输入:你好!
模型输出:你好呀,很高兴见到你!
```

**(2)模型实际看到的样子** —— 被特殊字符包裹、标好角色的一长串 token:

```
<|im_start|>user  user  你好!  <|im_start|>  assistant  │  你好呀,很高兴见到你!  <|im_end|>
└──────────── 用户输入部分 ────────────┘         └──────── 模型输出部分 ────────┘
```

逐个拆解这串特殊字符的作用:

| 特殊字符 | 含义 |
|---|---|
| `<|im_start|>` | **一段文本开始**(im = input message) |
| `user` | **消息身份(角色)**,标明这段话是"用户"说的;用于构建**多轮对话**时区分谁在说 |
| `你好!` | 用户的真实输入内容 |
| `<|im_end|>` | **一段文本结束**(即用户输入到此为止) |
| `<|im_start|>` | **新一段文本开始** |
| `assistant` | 角色为"助手",表示**接下来由模型来创建消息** |
| `你好呀,很高兴见到你!` | 模型生成的真实回答 |
| `<|im_end|>` | 模型创建消息的**结束** |

> [!key-insight] 模型靠这组标记"规范自己的行为"
> 模型正是通过这一组特殊字符,来**判断当前该谁说话、说到哪结束**:看到 `assistant` 就知道"轮到我生成了",生成完打出 `<|im_end|>` 就知道"我这轮说完了"。
> - `<|im_start|>` / `<|im_end|>`:管**起止**(一段话从哪开始、到哪结束)。
> - `user` / `assistant` / `system`:管**身份**(这段话是谁说的),多轮对话就是靠它一轮轮叠起来的。

### 这些特殊字符去哪查:tokenizer_config.json

模型靠这组特殊标记来**判断消息类型**(当前该谁说),也靠**输出特殊标记来确定停止时间**(打出 `<|im_end|>` 就停)。

那一个模型到底有哪些特殊字符、提示词模板长什么样?**绝大多数模型都能在自己的 `tokenizer_config.json` 里查到完整的特殊标记符(以及系统提示词模板 Chat Template)。**

> [!key-insight] 造数据集前,先去翻 tokenizer_config.json
> 想给某个模型造微调数据,**第一步就是打开它的 `tokenizer_config.json`**,看清楚:
> - 它用哪些特殊 token(`<|im_start|>`?`[INST]`?各家不同)。
> - 它的 `chat_template` 字段定义了消息怎么拼接。
> 照着这套规范组织数据,模型才"读得懂、学得进"。**这是从"原理"落到"动手造数据"的关键一跳。**

## 底层原理二:实际数据集长什么样(以 alpaca_zh 为例)

实际微调用的是**有监督数据集**,也就是要喂给模型一条条 **QA 对(问题-答案)**。最著名的中文微调数据集之一 **alpaca_zh** 就是这种格式,每条样本是一个含三个字段的 JSON 对象:

```json
[
  {
    "instruction": "识别并解释给定列表中的两个科学理论:细胞理论和日心说。",
    "input": "",
    "output": "细胞理论是生物科学的一个理论,它认为所有生命体都是由微小的基本单元——细胞所构成……"
  },
  {
    "instruction": "输入三支篮球队的名称并生成一个适当的口号。",
    "input": "输入:俄克拉荷马城雷霆队,芝加哥公牛队,布鲁克林网队。",
    "output": "输出:\"雷霆、公牛和网队:各显神通,角逐群雄!\""
  }
]
```

三个字段的分工:

| 字段 | 作用 | 类比 |
|---|---|---|
| **instruction** | 给模型的**指令/任务**(要它干什么) | 老师布置的题目要求 |
| **input** | 任务的**补充输入**(可为空 `""`) | 题目附带的材料/数据 |
| **output** | 期望模型给出的**标准答案** | 标准答案,模型要学着往这个方向答 |

> [!key-insight] alpaca 格式 = "指令 + 输入 → 输出" 的监督样本
> - 当任务**自带要处理的内容**时,放进 `input`(如"给这三支球队起口号" → 球队名放 input)。
> - 当指令**本身就说清楚了**、不需要额外材料时,`input` 留空 `""`(如"解释细胞理论")。
> - **监督信号全在 `output`**:微调就是反复告诉模型"遇到这个 instruction(+input),你应该输出这个 output"。
> 这套人类可读的 alpaca 格式,**最终还是会被框架按上一节的 Chat Template(`<|im_start|>` 那套)自动转成模型能读的 token 序列** —— 你写数据时用 alpaca 这种好懂的格式,套模板的脏活交给框架。

### 一条数据从 alpaca 到 token 的完整转换

把前两节串起来看一个完整例子。一条 alpaca 格式数据(`instruction` 为空,只有 input/output):

```json
{
  "instruction": "",
  "input": "输入:你好。",
  "output": "输出:你好,有什么可以帮到你的?"
}
```

针对 Qwen3 微调时,**微调脚本会把这条数据(无论原始什么格式)自动转成模型的模板格式**:

```
<|im_start|>user\n你好<|im_end|>\n<|im_start|>assistant\n你好,有什么可以帮到你的?<|im_end|>
```

> [!key-insight] 训练的本质:学习 assistant 之后该说什么
> 实际训练时,模型会**根据 `assistant` 之前的内容(用户那部分),学习 `assistant` 之后的内容(标准答案)**。
> - `assistant` 标记之前 = **条件(给定的上文)**,模型只读不学。
> - `assistant` 标记之后到 `<|im_end|>` = **要学的目标**,模型反复调整参数,让自己在这个上文下能生成出这段答案。
> 这也解释了为什么 `output` 是监督信号的核心 —— 它就是"`assistant` 之后那段",是模型真正要学会输出的东西。
> **一句话:你写好 instruction/input/output → 脚本套模板 → 模型学"看到 user 这段,就该接 assistant 这段"。**

## 底层原理三:带系统提示的数据集(instruction 去哪了)

上面例子里 `instruction` 是空的。但很多数据集 `instruction` 字段是**有内容**的,它到底如何带入微调?**答案还是靠特殊字符 —— 它被映射成 `system`(系统提示)那段。**

看一条三字段都填满的对话:

| 字段 | 内容 | 实际角色 |
|---|---|---|
| **instruction** | 你是一名助人为乐的助手。 | → **系统提示词(system)** |
| **input** | 你好,好久不见。 | → 用户输入(user) |
| **output** | 是的呀,好久不见,最近有什么有趣的事情要和我分享么? | → 助手回复(assistant) |

微调脚本会把它转成**三段式**模板格式:

```
<|im_start|>system
你是一名助人为乐的助手。<|im_end|>
<|im_start|>user
你好,好久不见。<|im_end|>
<|im_start|>assistant
是的呀,好久不见,最近有什么有趣的事情要和我分享么?<|im_end|>
```

> [!key-insight] instruction → system,这是关键映射
> - **`instruction` 字段不是和 input 拼在一起,而是单独成为 `<|im_start|>system … <|im_end|>` 这一段**,用来设定模型的"人设 / 角色 / 规则"。
> - 所以一条数据其实对应**三种角色**:`instruction`→system(背景设定)、`input`→user(用户这轮说的)、`output`→assistant(模型该答的)。
> - 训练逻辑不变:**还是以 `assistant` 为界,在"system + user"这些上文条件下,学习 `assistant` 之后该输出什么。** 只是这次上文里多了一段 system 人设。
> 这就是为什么带 `instruction` 的数据能让模型"记住自己是谁"—— 它被实打实编码进了 system 段,每轮都先读到。

## 底层原理四:带 Function Calling 时,先有 tool schema

更进一步,如果对话过程中带入了 **Function Calling(工具调用)**,模型在生成前会**先读取提前准备好的 `tool schema`(工具定义清单)** —— 也可能是自动生成的,例如 **MCP** 就能自动创建 tool schema。

一个查天气工具的 schema 长这样:

```json
tool_schema = [{
    "name": "get_weather",
    "description": "查询指定城市的天气信息",
    "parameters": {
        "type": "object",
        "properties": {
            "location": {
                "type": "string",
                "description": "要查询天气的城市名称"
            }
        },
        "required": ["location"]
    }
}]
```

逐字段看,tool schema 本质是**用结构化 JSON 把"有什么工具、怎么用"讲给模型听**:

| 字段 | 作用 |
|---|---|
| **name** | 工具的函数名(`get_weather`),模型决定调用时填这个名字 |
| **description** | 工具是干什么的 —— **模型靠它判断"该不该用这个工具"** |
| **parameters** | 工具的入参定义(JSON Schema 格式) |
| └ **properties** | 每个参数的名字、类型(`type`)、说明(`description`) |
| └ **required** | 哪些参数是**必填**的(这里 `location` 必填) |

> [!key-insight] tool schema 是工具的"说明书",喂在对话最前面
> - Function Calling 数据集,比前面多了一层:**对话开始前先把 tool schema 告诉模型**(通常并入 system 段或专门的 tools 段),模型才知道"我手上有哪些工具、各自怎么调"。
> - 模型读完 schema 后,在合适时机**输出一段结构化的调用请求**(如 `{"name":"get_weather","arguments":{"location":"北京"}}`),由外部程序真正执行,再把结果回填给模型。
> - 所以**造 Function Calling 微调数据**,核心就是教模型:**"看到这种问题(天气) + 手上有这个工具(get_weather)→ 应该输出这样一段规范的调用"**。`description` 写得越清楚,模型越不会乱调或漏调。
> 这就是 [[07 微调数据集的创建原理|本篇开头]]说的"想给 Qwen3 微调 Function Calling 能力"为什么往往要**自己造数据** —— 公开集很难恰好覆盖你那套自定义工具。

### Function Calling 数据集长什么样(一条完整样本)

把 tool schema 和三字段结合,一条带工具调用的训练数据是这样:

| 字段 | 内容 |
|---|---|
| **instruction(system)** | 你是一名助人为乐的助手。**当用户查询天气的时候,请调用 `get_weather` 函数进行天气信息查询。** |
| **input(user)** | 你好,请帮我查询下北京天气。 |
| **output(assistant)** | `{"name": "get_weather", "arguments": {"location": "北京"}}` |

> [!key-insight] 关键:此时 output 不是自然语言,而是一条 Function call message
> - 注意 `output` 不再是"北京今天晴…"这种人话,而是一段**结构化的调用指令**(函数名 + 参数)。这就是所谓的 **Function call message**。
> - 训练目标也随之变了:模型要学会的是 **"看到天气类提问 + system 里允许调用 get_weather → 输出这段调用 JSON"**,而不是直接编一个天气答案。
> - `system` 里那句"当用户查询天气时请调用 get_weather"是**触发条件**;`output` 的调用 JSON 是**期望动作**。二者配套,模型才学得会"何时调、怎么调"。

模型给出这条 Function call message 后,真正的天气查询由外部程序执行,**再把结果回填**继续对话(下一节展开"模型真实的输入和输出"长什么样)。

### 模型真实看到的输入:tool schema 塞进 system 段

上面那条人类可读的数据,套上模板后**模型真正读到的 system 段**长这样(以 Qwen3 为例):

```
<|im_start|>system
你是一名助人为乐的助手。当用户查询天气的时候,请调用get_weather函数进行天气信息查询。

# Tools

You may call one or more functions to assist with the user query.

You are provided with function signatures within <tools></tools> XML tags:
<tools>
{"name": "get_weather", "description": "查询指定城市的天气信息", "parameters": {"type": "object", "properties": {"location": {"type": "string", "description": "要查询天气的城市名称"}}, "required": ["location"]}}
</tools>

For each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:
<tool_call>
{"name": <function-name>, "arguments": <args-json-object>}
</tool_call>
<|im_end|>
```

> [!key-insight] 看懂这段"塞进 system 的工具说明书"
> 模型真实读到的 system 段,其实是**三部分拼起来**的:
> 1. **你写的人设/规则**(你是助手,查天气就调 get_weather)。
> 2. **`# Tools` 区块**:用 `<tools>…</tools>` XML 标签把上一节的 tool schema 原样塞进来,告诉模型"你有哪些函数可用"。
> 3. **调用格式约定**:明确要求模型"要调用就用 `<tool_call>…</tool_call>` 标签包一段 `{name, arguments}` JSON 返回"。
> 也就是说:**你只在数据里写一句"查天气请调 get_weather"+ 一份 tool schema,框架的模板会自动把它膨胀成上面这一大段规范化 system 提示。** 模型就是靠这段,既知道"有什么工具",又知道"该用什么格式调"。

模型给出 `<tool_call>` 后,外部程序执行函数、把结果回填,对话才继续。

### 完整一条 Function Calling 训练样本(从 system 到 assistant)

把 system + user + assistant 全拼起来,**模型真实看到的完整序列**是这样:

```
<|im_start|>system
你是一名助人为乐的助手。当用户查询天气的时候,请调用get_weather函数进行天气信息查询。

# Tools

You may call one or more functions to assist with the user query.

You are provided with function signatures within <tools></tools> XML tags:
<tools>
{"name": "get_weather", "description": "查询指定城市的天气信息", "parameters": {"type": "object", "properties": {"location": {"type": "string", "description": "要查询天气的城市名称"}}, "required": ["location"]}}
</tools>

For each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:
<tool_call>
{"name": <function-name>, "arguments": <args-json-object>}
</tool_call>
<|im_end|>
<|im_start|>user
你好,请帮我查询下北京天气。<|im_end|>
<|im_start|>assistant
<tool_call>
{"name": "get_weather", "arguments": {"location": "北京"}}
</tool_call><|im_end|>
```

> [!key-insight] 训练目标:学会输出 assistant 后那段 `<tool_call>`
> 整条样本里,**模型只学 `<|im_start|>assistant` 之后到 `<|im_end|>` 那段**(上面最后那个 `<tool_call>{...}</tool_call>`),前面 system+user 全是给定上文。
> - system 段的格式约定里 `<function-name>` / `<args-json-object>` 是**占位模板**;assistant 段把它**填成真值**:`get_weather` + `{"location": "北京"}`。
> - 所以 Function Calling 微调,本质就是用一批这样的样本,反复教模型:**"在给定工具清单 + 用户提问的上文下,严格按 `<tool_call>` 格式吐出正确的函数名和参数。"**
> - 格式一旦学歪(标签错、JSON 不合法、参数名对不上 schema),外部程序就解析不了、工具就调不通 —— 这也是为什么 FC 数据的**格式规范性**比普通对话数据更苛刻。

## 底层原理五:带思考过程(思考链)的数据集结构

现在很多模型是**推理模型 / 混合推理模型**(如 Qwen3、DeepSeek-R1),回答前会先"想一段"。要微调出这种能力,数据里就得**额外带上思考过程**。这类数据集常用 `conversations` 多轮结构,并在 assistant 消息里多一个 `reasoning_content` 字段:

```json
{
  "system": "你是一名助人为乐的助手。",
  "conversations": [
    {"role": "user", "content": "你好,好久不见。"},
    {
      "role": "assistant",
      "content": "是的呀,好久不见,最近有什么有趣的事情要和我分享么?",
      "reasoning_content": "好的,用户发来\"你好,好久不见!\",我需要回应。首先,用户可能希望得到亲切的回……"
    }
  ]
}
```

和前面格式的对照:

| 要素 | 前面的 alpaca / FC 格式 | 这里的思考链格式 |
|---|---|---|
| 整体结构 | 扁平三字段(instruction/input/output) | `system` + `conversations` 列表(更接近多轮对话原貌) |
| 角色表示 | 靠字段名隐含 | 显式 `role: user/assistant` |
| 助手回复 | 只有 `output`(最终答案) | `content`(最终答案)**+ `reasoning_content`(思考过程)** 两段 |

> [!key-insight] 关键在多出来的 reasoning_content
> - **`content` = 给用户看的最终答案**;**`reasoning_content` = 模型回答前的内部思考(思维链 CoT)**。
> - 套模板后,思考过程通常会被包进 **`<think>…</think>`** 这类特殊标记里,放在最终答案之前。模型要学的,就是"先在 `<think>` 里推演,再给出 `content`"。
> - 所以**想微调出"会思考"的模型,数据就必须带高质量的 `reasoning_content`** —— 只给最终答案,模型学不会"怎么想";思考过程写得好不好,直接决定推理能力强不强。
> - 这也呼应本篇开头那个例子:想给 Qwen3 微调 Function Calling **同时保留混合推理能力**,就得造**既有 `tool_call` 又有 `reasoning_content`** 的复合数据 —— 公开集几乎没有现成的,只能自己造。

### content 和 reasoning_content 怎么合到一起:`<think>` 标记

上面 `content` 和 `reasoning_content` 在数据里是**两个分开的字段**,但模型实际要生成的是**一整段连起来的 output**。框架会把它拼成这样:

- **系统提示词(instruction)**:你是一名助人为乐的助手。
- **用户输入(input)**:你好,好久不见。
- **助手回复(output)**:
  ```
  <think>好的,用户发来"你好,好久不见!",我需要回应。首先,用户可能希望得到亲切的回应,所以应该用友好的语气。</think>\n是的呀,好久不见,最近有什么有趣的事情要和我分享么?
  ```

> [!key-insight] `<think>思考</think>\n最终答案` —— 推理模型 output 的标准形态
> - **`reasoning_content`(思考)被包进 `<think>…</think>`**,紧跟一个换行 `\n`,**后面才是 `content`(给用户的最终答案)**。两段在 output 里前后相连,合成模型真正要学的目标。
> - 这正印证了原理五的判断:思考链不是独立存在,而是**作为 output 的前半段、被特殊标记圈起来**。推理时,前端只需把 `<think>…</think>` 里的内容折叠/隐藏,用户就只看到后面的答案。
> - 所以造推理数据,本质是教模型:**"先在 `<think>` 里把推理写出来,`</think>\n` 之后再给结论。"** `<think>` 这对标记,就是"思考区"和"回答区"的分界线。

## 实操:Qwen3 混合推理数据集的构造方法

懂了数据集背后的原理,接下来的问题是:**到底怎么动手构造?** 一般思路是**"挑现成的 + 按需拼接 + 调配比"**。

**(1)去哪挑数据集**:在 **HuggingFace、ModelScope(魔搭)或 LLaMA-Factory** 的数据集库里挑合适的,再根据实际需要组装。

**(2)以"Qwen3 高效微调且保留混合推理"为例**,可以拼两类数据:

| 数据集 | 类型 | 作用 |
|---|---|---|
| **FineTome-100k** | 普通对话数据(无推理字段) | 保住模型的**通用对话/非推理能力**,防止只练数学把日常聊天练丢了 |
| **OpenMathReasoning** | 数学类、**带 `reasoning_content` 推理字段** | 提升模型的**数学推理能力**(教它"会想") |

> 来源:`huggingface.co/datasets/mlabonne/FineTome-100k`、`huggingface.co/datasets/nvidia/OpenMathReasoning`

**(3)拼接 + 调配比**:把这两个数据集**混在一起**训练,并在持续微调过程中**不断调整 "CoT 数学数据" 与 "普通文本问答数据" 的配比**,在提升数学能力的同时保留混合推理(既能深度思考、也能正常对话)。

> [!key-insight] 配比就是"既要又要"的平衡阀
> - **只喂数学推理数据** → 数学变强,但可能 [[05 灾难性遗忘|灾难性遗忘]] 通用对话能力(变成"只会做题的书呆子")。
> - **只喂普通对话数据** → 对话自然,但学不会深度推理。
> - **混合 + 调配比**,才能让模型"该思考时思考、该聊天时聊天"—— 这正是混合推理模型的精髓。
> 配比不是一次定死,而是**边训边看评测**([[01 EvalScope|EvalScope]] 在数学集和通用集上各打分),发现哪头退化了就调高那头的比例。**造数据 → 训 → 评 → 调配比 → 再训** 是一个循环。

## 继续学习

- [[01 四大微调框架概述|四大微调框架概述]](数据备好后,用哪个框架来训)
- [[04 SFT 与指令微调|SFT 与指令微调]](指令微调数据集最常见的"指令-回答"范式)
- [[06 微调所需软硬件环境|微调所需软硬件环境]](数据和框架都备齐后,还要看硬件够不够)
